local options = {
    ensure_installed = { "lua", "python", "vim", "vimdoc", "bash", "markdown", "markdown_inline", "c" },

    highlight = {
        enable = true,
        use_languagetree = true,
    },

    indent = { enable = true },
}

return options
